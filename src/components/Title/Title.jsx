import React from "react";

const Title = ({ children }) => {
    return <h2 className="text-4xl text-center md:text-5xl font-light uppercase text-acento-primario">{children}</h2>;
};

export default Title;
